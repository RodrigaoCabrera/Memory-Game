1 crea una interface de n cantidad de columnas y n cantidad de filas. Deben ser pares.

2 Cada tarjeta debe tener un símbolo e imagen y debe existir solo un par de la misma.

3 cuando clickee en una debe mostrar la imágen y luego permitir clickear en otra. Si la 
segunda es la misma debe mostrarlas durante todo el juego. Si son diferentes debe 
mostrarlas por unos segundos y luego ocultarlas, agregando un feedback al usuario que diga
que no son iguales.

4. Agregar a una lista en forma de ranking que muestre cuanto tiempo tardó el usuario en 
completar el juego y cuántos errores cometió.